apiVersion: v1
kind: Service
metadata:
  name: foobar
spec:
  type: NodePort
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
    nodePort: 30100
  selector:
    app: foobar
